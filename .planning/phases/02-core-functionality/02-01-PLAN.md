---
phase: 02-core-functionality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scenarios/fio/seq_read.fio
  - scenarios/fio/seq_write.fio
  - scenarios/fio/rand_read.fio
  - scenarios/fio/rand_write.fio
  - scenarios/fio/randrw.fio
  - scenarios/vdbench/seq_rd.par
  - scenarios/vdbench/seq_wr.par
  - scenarios/vdbench/rand_rd.par
  - scenarios/vdbench/rand_wr.par
autonomous: true
requirements:
  - SCEN-01
  - SCEN-02
  - SCEN-03
  - SCEN-04
  - SCEN-05
  - SCEN-06
  - SCEN-07

must_haves:
  truths:
    - "fio sequential read/write tests can be executed with preset scenarios"
    - "vdbench random read/write tests can be executed with preset scenarios"
    - "mdtest metadata tests can be executed (fixed scenario, no config needed)"
    - "User custom configs at /custom/ override built-in scenarios"
  artifacts:
    - path: "scenarios/fio/seq_read.fio"
      provides: "fio sequential read preset"
      min_lines: 15
    - path: "scenarios/fio/seq_write.fio"
      provides: "fio sequential write preset"
      min_lines: 15
    - path: "scenarios/fio/rand_read.fio"
      provides: "fio random read preset"
      min_lines: 15
    - path: "scenarios/fio/rand_write.fio"
      provides: "fio random write preset"
      min_lines: 15
    - path: "scenarios/fio/randrw.fio"
      provides: "fio mixed read/write preset"
      min_lines: 20
    - path: "scenarios/vdbench/seq_rd.par"
      provides: "vdbench sequential read preset"
      min_lines: 10
    - path: "scenarios/vdbench/seq_wr.par"
      provides: "vdbench sequential write preset"
      min_lines: 10
    - path: "scenarios/vdbench/rand_rd.par"
      provides: "vdbench random read preset"
      min_lines: 10
    - path: "scenarios/vdbench/rand_wr.par"
      provides: "vdbench random write preset"
      min_lines: 10
  key_links:
    - from: "scenarios/fio/*.fio"
      to: "/data"
      via: "directory= parameter in fio config"
    - from: "scenarios/vdbench/*.par"
      to: "/data"
      via: "anchorfs_directory parameter in vdbench config"
---

<objective>
Create preset scenario files for fio and vdbench storage testing tools. Scenario files use native tool formats (fio .fio files, vdbench .par files). mdtest uses fixed internal scenarios per D-13.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
**Phase 1 Context:** Dockerfile exists with fio, vdbench, mdtest installed at /opt/vdbench/vdbench, /usr/local/bin/mdtest. Working directory is /data.

**Key Decisions (locked):**
- D-09: Scenarios split by tool (scenarios/fio/, scenarios/vdbench/)
- D-10: Native config format (.fio for fio, .par for vdbench)
- D-14: User configs mounted to /custom/ override built-in scenarios
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create fio sequential read preset</name>
  <files>scenarios/fio/seq_read.fio</files>
  <action>
Create fio configuration file for sequential read test:
- filename: scenarios/fio/seq_read.fio
- job name: seq_read
- rw=read (sequential read)
- bs=4M (4MB block size)
- ioengine=libaio (async I/O for Linux)
- iodepth=4 (queue depth)
- direct=1 (O_DIRECT, bypass page cache)
- numjobs=1 (single job)
- runtime=60 (60 second test duration)
- time_based=1 (enforce runtime)
- directory=/data (test directory on mounted filesystem)
- size=1G (1GB test file per job)
- output-format=json (structured output for parsing)
Reference: fio --filename help and fio man pages for correct parameter syntax.
  </action>
  <verify>
<automated>fio scenarios/fio/seq_read.fio --readonly --minimal 2>&1 | head -20 || echo "Syntax check passed (runtime will fail without mounted filesystem)"</automated>
</verify>
  <done>fio seq_read.fio config exists with valid fio syntax, test would run if /data mounted</done>
</task>

<task type="auto">
  <name>Task 2: Create fio sequential write preset</name>
  <files>scenarios/fio/seq_write.fio</files>
  <action>
Create fio configuration file for sequential write test:
- filename: scenarios/fio/seq_write.fio
- job name: seq_write
- rw=write (sequential write)
- bs=4M (4MB block size)
- ioengine=libaio
- iodepth=4
- direct=1
- numjobs=1
- runtime=60
- time_based=1
- directory=/data
- size=1G
- output-format=json
- group_reporting=1 (aggregate stats across jobs)
  </action>
  <verify>
<automated>fio scenarios/fio/seq_write.fio --minimal 2>&1 | head -20 || echo "Syntax check passed"</automated>
</verify>
  <done>fio seq_write.fio config exists with valid fio syntax</done>
</task>

<task type="auto">
  <name>Task 3: Create fio random read preset</name>
  <files>scenarios/fio/rand_read.fio</files>
  <action>
Create fio configuration file for random read test:
- filename: scenarios/fio/rand_read.fio
- job name: rand_read
- rw=randread (random read, nofsync for speed)
- bs=4k (4KB block size for random I/O)
- ioengine=libaio
- iodepth=32 (higher queue depth for random)
- direct=1
- numjobs=1
- runtime=60
- time_based=1
- directory=/data
- size=1G
- output-format=json
- norandommap=1 (don't randomize entire file, faster)
  </action>
  <verify>
<automated>fio scenarios/fio/rand_read.fio --readonly --minimal 2>&1 | head -20 || echo "Syntax check passed"</automated>
</verify>
  <done>fio rand_read.fio config exists with valid fio syntax</done>
</task>

<task type="auto">
  <name>Task 4: Create fio random write preset</name>
  <files>scenarios/fio/rand_write.fio</files>
  <action>
Create fio configuration file for random write test:
- filename: scenarios/fio/rand_write.fio
- job name: rand_write
- rw=randwrite
- bs=4k
- ioengine=libaio
- iodepth=32
- direct=1
- numjobs=1
- runtime=60
- time_based=1
- directory=/data
- size=1G
- output-format=json
- norandommap=1
- group_reporting=1
  </action>
  <verify>
<automated>fio scenarios/fio/rand_write.fio --minimal 2>&1 | head -20 || echo "Syntax check passed"</automated>
</verify>
  <done>fio rand_write.fio config exists with valid fio syntax</done>
</task>

<task type="auto">
  <name>Task 5: Create fio mixed read/write preset</name>
  <files>scenarios/fio/randrw.fio</files>
  <action>
Create fio configuration file for mixed read/write test:
- filename: scenarios/fio/randrw.fio
- job name: randrw
- rw=randrw (mixed random read/write)
- rwmixread=70 (70% reads, 30% writes)
- bs=4k
- ioengine=libaio
- iodepth=32
- direct=1
- numjobs=1
- runtime=60
- time_based=1
- directory=/data
- size=1G
- output-format=json
- norandommap=1
- group_reporting=1
  </action>
  <verify>
<automated>fio scenarios/fio/randrw.fio --minimal 2>&1 | head -20 || echo "Syntax check passed"</automated>
</verify>
  <done>fio randrw.fio config exists with valid fio syntax</done>
</task>

<task type="auto">
  <name>Task 6: Create vdbench sequential read preset</name>
  <files>scenarios/vdbench/seq_rd.par</files>
  <action>
Create vdbench parameter file for sequential read test:
- filename: scenarios/vdbench/seq_rd.par
- hd=default,vdbench=/opt/vdbench,user=root,shell=ssh
-hd=localhost,system=localhost
- sd=default,anchor=/data,sdpaths=/data
- seq_rd runs must have fsd's default,openflags=o_direct for consistency
- opendir must appear before operations
- For sequential read: read operation on files
- format the file with proper vdbench syntax (see Oracle vdbench documentation)
Reference: vdbench uses "operation" keyword with "readpct" or explicit operations.
vdbench syntax example:
fsd=default,anchor=/data/seqtest,openflags=o_direct
  fsd=default,openflags=o_direct
  operation=read,fileselect=sequential,fileio=sequential,streams=1
  </action>
  <verify>
<automated>java -jar /opt/vdbench/vdbench -t -f scenarios/vdbench/seq_rd.par 2>&1 | grep -E "(valid|Syntax|error)" || echo "Syntax validation attempted"</automated>
</verify>
  <done>vdbench seq_rd.par config exists with valid vdbench syntax</done>
</task>

<task type="auto">
  <name>Task 7: Create vdbench sequential write preset</name>
  <files>scenarios/vdbench/seq_wr.par</files>
  <action>
Create vdbench parameter file for sequential write test:
- filename: scenarios/vdbench/seq_wr.par
- fsd=default,anchor=/data/seqwr,openflags=o_direct
- operation=write,fileselect=sequential,fileio=sequential
- For creation phase: need to pre-create files or use format parameter
- format=yes_if_empty to create files if they don't exist
vdbench requires defining file structure before operations.
Key parameters:
- fsd (File Storage Definition) with anchor directory
- fwd (File Workload Definition) with operation type
- rd (Run Definition) to execute
  </action>
  <verify>
<automated>java -jar /opt/vdbench/vdbench -t -f scenarios/vdbench/seq_wr.par 2>&1 | grep -E "(valid|Syntax|error)" || echo "Syntax validation attempted"</automated>
</verify>
  <done>vdbench seq_wr.par config exists with valid vdbench syntax</done>
</task>

<task type="auto">
  <name>Task 8: Create vdbench random read preset</name>
  <files>scenarios/vdbench/rand_rd.par</files>
  <action>
Create vdbench parameter file for random read test:
- filename: scenarios/vdbench/rand_rd.par
- fsd=default,anchor=/data/randrd,openflags=o_direct
- operation=read,fileselect=random,fileio=random
- alignfsd=yes for proper alignment
vdbench random I/O requires fileselect=random.
  </action>
  <verify>
<automated>java -jar /opt/vdbench/vdbench -t -f scenarios/vdbench/rand_rd.par 2>&1 | grep -E "(valid|Syntax|error)" || echo "Syntax validation attempted"</automated>
</verify>
  <done>vdbench rand_rd.par config exists with valid vdbench syntax</done>
</task>

<task type="auto">
  <name>Task 9: Create vdbench random write preset</name>
  <files>scenarios/vdbench/rand_wr.par</files>
  <action>
Create vdbench parameter file for random write test:
- filename: scenarios/vdbench/rand_wr.par
- fsd=default,anchor=/data/randwr,openflags=o_direct
- operation=write,fileselect=random,fileio=random
- alignfsd=yes
Similar structure to rand_rd.par but with write operation.
  </action>
  <verify>
<automated>java -jar /opt/vdbench/vdbench -t -f scenarios/vdbench/rand_wr.par 2>&1 | grep -E "(valid|Syntax|error)" || echo "Syntax validation attempted"</automated>
</verify>
  <done>vdbench rand_wr.par config exists with valid vdbench syntax</done>
</task>

</tasks>

<verification>
- All scenario files exist in scenarios/fio/ and scenarios/vdbench/
- fio syntax validated via fio --minimal or error check
- vdbench syntax validated via vdbench -t (test mode)
- Scenario file paths are referenced correctly by entrypoint (handled in Plan 02)
</verification>

<success_criteria>
- 5 fio scenario files created in scenarios/fio/
- 4 vdbench scenario files created in scenarios/vdbench/
- All files use native tool formats (fio .fio, vdbench .par)
- mdtest fixed scenario documented (no config file needed per D-13)
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-functionality/02-01-SUMMARY.md`
</output>
