---
name: 08-01-PLAN.md
description: Add -t int integration test command support
type: plan
phase: 8
phase_slug: integration-basics
depends_on: []
wave: 1
requirements_addressed: [INTG-01, AUTO-01]
files_modified:
  - entrypoint.sh
  - dingofs-testsuite-tool
created: 2026-04-21
status: planned
---

# Phase 8: 集成测试基础

## Plan 08-01: 添加 `-t int` 集成测试命令支持

**Objective:** 用户可以使用 `dtt -t int` 运行 DingoFS 自动化框架测试

**Requirements Addressed:**
- INTG-01: 添加 `-t int` 命令行参数支持
- AUTO-01: 集成 dingofs-integration-test 到镜像

**Success Criteria:**
1. `dtt -t int` 在容器内被识别为有效工具
2. `integration_run()` 函数存在并可被调用
3. dingofs-integration-test 存在于镜像 /dingofs-integration-test
4. `dtt -t int --help` 显示帮助信息

---

## Task 1: 添加 integration_run() 函数到 entrypoint.sh

### Action
在 entrypoint.sh 中添加 `integration_run()` 函数来处理 `-t int` 测试执行。

**Read first:**
- `/mnt/disk5/daigy/dingofs-testsuite-tool/entrypoint.sh` — 现有的 ltp_run() 和 pjdtest_run() 实现作为参考模式
- `/mnt/disk5/daigy/dingofs-testsuite-tool/dingofs-integration-test/run_tests.py` — 了解自动化框架的运行方式

**具体修改:**

1. 在 `TOOL` 变量声明附近添加 `INTEGRATION_DIR="/dingofs-integration-test"`

2. 在 `show_ltp_help()` 函数后添加 `show_int_help()` 函数:

```bash
show_int_help() {
    cat << EOF
INT 集成测试 (DingoFS Automation Framework)
============================================

用法: dtt -t int -s <场景> [-m <挂载点>] [-o <输出目录>]

测试模块:
  quota       - Quota 配额测试 (默认)
  client      - Client 客户端测试
  cache_node  - Cache Node 缓存节点测试
  chaos       - Chaos 混沌测试

示例:
  # 运行所有 quota 测试 (默认)
  dtt -t int -s quota

  # 运行 client 测试
  dtt -t int -s client

  # 运行所有集成测试
  dtt -t int -s all

注意: 需要配置 MDS 地址 (dtt config set mdsaddr)
EOF
}
```

3. 在 `dispatch_tool()` 函数的 case 语句中添加:

```bash
        int|integration)
            integration_run
            ;;
```

4. 在 `ltp_run()` 函数后添加 `integration_run()` 函数:

```bash
integration_run() {
    echo "Running DingoFS Integration Tests..."
    echo "  Module: $SCENARIO"
    echo "  Mount: $MOUNT"
    echo "  Output: $OUTPUT"
    echo ""

    # Get MDS address from environment
    local mdsaddr="${MDSADDR:-}"
    if [[ -z "$mdsaddr" ]]; then
        echo "Error: MDSADDR environment variable not set"
        echo "Please set MDS address via: dtt config set mdsaddr <address>"
        exit 1
    fi

    # Create output directory
    mkdir -p "$OUTPUT"
    mkdir -p "$OUTPUT/integration"

    local start_time=$(date +"%Y-%m-%d %H:%M:%S")
    local timestamp=$(date +"%Y%m%d_%H%M%S")
    local log_file="$OUTPUT/integration/int_${timestamp}.log"

    echo "Log file: $log_file"
    echo ""

    # Run the integration test framework
    # Default module is quota if not specified
    local module="${SCENARIO:-quota}"

    cd "$INTEGRATION_DIR"

    # Run tests and capture output
    if python3 run_tests.py "$module" --env env_126_quota --skip-setup 2>&1 | tee "$log_file"; then
        local exit_code=0
    else
        local exit_code=$?
    fi

    echo ""
    echo "Integration tests completed with exit code: $exit_code"
    echo "Log saved to: $log_file"

    # Log result
    log_result "int" "$module" "$exit_code" "$start_time" "$OUTPUT"

    exit $exit_code
}
```

5. 在 `validate_params()` 函数中，将 TOOL 验证从:
```bash
elif [[ ! "$TOOL" =~ ^(fio|vdbench|mdtest|pjdtest|ltp)$ ]]; then
```
修改为:
```bash
elif [[ ! "$TOOL" =~ ^(fio|vdbench|mdtest|pjdtest|ltp|int)$ ]]; then
```

6. 在 `scenario_exists()` 函数的 case 语句中添加:
```bash
        int|integration)
            # int scenarios: quota, client, cache_node, chaos, all
            [[ "$scenario" == "all" ]] || [[ "$scenario" =~ ^(quota|client|cache_node|chaos)$ ]]
            ;;
```

### Acceptance Criteria
- `entrypoint.sh` 包含 `integration_run()` 函数
- `entrypoint.sh` 包含 `show_int_help()` 函数
- `dispatch_tool()` 处理 `int` 和 `integration` 工具名
- `validate_params()` 接受 `int` 作为有效工具
- `scenario_exists()` 接受 quota/client/cache_node/chaos/all 作为有效场景

---

## Task 2: 更新 dingofs-testsuite-tool 帮助信息

### Action
更新 `dingofs-testsuite-tool` 脚本，添加 `int` 到工具列表和验证逻辑中。

**Read first:**
- `/mnt/disk5/daigy/dingofs-testsuite-tool/dingofs-testsuite-tool`

**具体修改:**

1. 在 `show_help()` 函数的工具列表中添加 `int`:

```
工具:
  fio       - 灵活I/O测试工具 (存储性能测试)
  vdbench   - Oracle存储测试工具
  mdtest    - MPI文件系统元数据测试
  pjdtest   - POSIX文件系统测试套件
  ltp       - Linux测试项目 (内核测试套件)
  int       - DingoFS集成测试 (自动化框架)
```

2. 在 `run_testsuite()` 函数的场景验证中，将:
```bash
if [[ -z "$scenario" ]]; then
    echo "Error: -s <scenario> is required for fio, vdbench, mdtest, pjdtest, and ltp"
    return 1
fi
```
修改为:
```bash
if [[ -z "$scenario" ]]; then
    echo "Error: -s <scenario> is required for fio, vdbench, mdtest, pjdtest, ltp, and int"
    return 1
fi
```

3. 在同一函数的工具名检查中更新错误消息:
```bash
echo "Error: -t <tool> is required (fio, vdbench, mdtest, pjdtest, ltp)"
```
为:
```bash
echo "Error: -t <tool> is required (fio, vdbench, mdtest, pjdtest, ltp, int)"
```

### Acceptance Criteria
- 帮助信息中显示 `int` 工具
- 运行 `dtt -t int --help` 时显示集成测试帮助

---

## Task 3: 验证 dingofs-integration-test 在镜像中存在

### Action
确认 Dockerfile 已经正确配置将 dingofs-integration-test 复制到镜像中。

**Read first:**
- `/mnt/disk5/daigy/dingofs-testsuite-tool/Dockerfile`

**验证项:**
- 检查 Dockerfile 包含 `COPY dingofs-integration-test /dingofs-integration-test`
- 检查 requirements.txt 已安装: `pip3 install ... -r requirements.txt`

**Note:** 这些已经在 Dockerfile 第 137-141 行存在:
```dockerfile
COPY dingofs-integration-test /dingofs-integration-test
RUN cd /dingofs-integration-test && \
    pip3 install --no-cache-dir --break-system-packages -r requirements.txt && \
    chmod +x run_tests.py
```

### Acceptance Criteria
- Dockerfile 包含将 dingofs-integration-test 复制到 /dingofs-integration-test 的指令
- requirements.txt 被安装

---

## Task 4: 添加 --help 支持到 entrypoint.sh 的 parse_args()

### Action
在 entrypoint.sh 的参数解析中，当 TOOL 是 `int` 时，调用 `show_int_help`。

**Read first:**
- `/mnt/disk5/daigy/dingofs-testsuite-tool/entrypoint.sh` (parse_args 函数部分，约 line 450-465)

**具体修改:**

在 parse_args() 的 -t|--tool 处理中，添加 int 和 integration 到工具帮助检查:

```bash
                if [[ "$3" == "--help" ]]; then
                    case "$TOOL" in
                        fio) show_fio_help; exit 0 ;;
                        vdbench) echo "vdbench: use --help for general help"; exit 0 ;;
                        mdtest) show_mdtest_help; exit 0 ;;
                        pjdtest) show_pjdtest_help; exit 0 ;;
                        ltp) show_ltp_help; exit 0 ;;
                        int|integration) show_int_help; exit 0 ;;
                        *) echo "Unknown tool: $TOOL"; exit 1 ;;
                    esac
                fi
```

### Acceptance Criteria
- `entrypoint.sh -t int --help` 显示集成测试帮助
- `entrypoint.sh -t integration --help` 显示集成测试帮助

---

## Verification

After all tasks complete:

1. Build the Docker image:
   ```bash
   docker build -t localhost/dingofs-testsuite-tools:test .
   ```

2. Verify the integration test directory exists in the image:
   ```bash
   docker run --rm localhost/dingofs-testsuite-tools:test ls -la /dingofs-integration-test/
   ```

3. Verify help works:
   ```bash
   docker run --rm localhost/dingofs-testsuite-tools:test -t int --help
   ```

4. Verify validate_params accepts 'int':
   ```bash
   docker run --rm localhost/dingofs-testsuite-tools:test -t int -s quota -m /data -o /data 2>&1 | head -20
   # Should show "MDSADDR environment variable not set" error (not "Invalid tool" error)
   ```

---

## Wave Summary

| Wave | Tasks | What it builds |
|------|-------|----------------|
| 1 | 1, 2, 3, 4 | Complete `-t int` integration test support |

