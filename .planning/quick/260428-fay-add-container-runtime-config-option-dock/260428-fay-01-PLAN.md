---
phase: quick
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/jenkins/.local/bin/dingofs-testsuite-tool
autonomous: true
requirements: []
must_haves:
  truths:
    - "User can set container runtime to docker or podman"
    - "All docker commands respect the configured runtime"
    - "Runtime defaults to docker if not set"
  artifacts:
    - path: "/home/jenkins/.local/bin/dingofs-testsuite-tool"
      provides: "Runtime config support with fallback default"
      min_lines: 567
      contains: "RUNTIME_FILE"
  key_links:
    - from: "load_config()"
      to: "RUNTIME_FILE"
      via: "case statement"
      pattern: "runtime.*cat.*RUNTIME_FILE"
    - from: "docker_cmd"
      to: "$(load_config runtime)"
      via: "variable substitution"
      pattern: "\\$\\(load_config runtime\\)"
---

<objective>
Add container runtime config option (docker/podman) to dtt wrapper script.

Purpose: Allow users to choose container runtime instead of hardcoded docker.
Output: Runtime config stored in ~/.dingofs_testsuite/runtime, respected by all docker commands.
</objective>

<context>
@/home/jenkins/.local/bin/dingofs-testsuite-tool
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add runtime config support</name>
  <files>/home/jenkins/.local/bin/dingofs-testsuite-tool</files>
  <action>
    Add to the script:

1. Add RUNTIME_FILE variable after other config files (line ~19):
   RUNTIME_FILE="$CONFIG_DIR/runtime"

2. Add "runtime" case in load_config() function (after email case):
   runtime)
       [[ -f "$RUNTIME_FILE" ]] && cat "$RUNTIME_FILE" || echo "docker"
       ;;

3. Add "runtime" case in save_config() function (after email case):
   runtime)
       echo "$value" > "$RUNTIME_FILE"
       ;;

4. Add "runtime" to config set validation (line ~218):
   testdir|output|image|mdsaddr|webhook_url|email runtime)

5. Add runtime to config show output (line ~236):
   local runtime=$(load_config runtime)
   ...
   echo " runtime: ${runtime:-docker}"

6. Add help text in show_help() - add to config set examples:
   dtt config set runtime docker   # or podman

Do NOT replace any docker commands yet - just add the config storage/retrieval infrastructure.
  </action>
  <verify>
    grep -c "RUNTIME_FILE" /home/jenkins/.local/bin/dingofs-testsuite-tool
    grep -c "load_config runtime" /home/jenkins/.local/bin/dingofs-testsuite-tool
  </verify>
  <done>Runtime config infrastructure exists, config show displays runtime</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded docker with runtime variable</name>
  <files>/home/jenkins/.local/bin/dingofs-testsuite-tool</files>
  <action>
    Replace ALL hardcoded "docker" commands with the runtime variable. At the top of each function that calls docker, load the runtime:

1. In cmd_debug() - line 277:
   local runtime=$(load_config runtime)
   local docker_cmd=("$runtime" "run" ...)

2. In run_docker_help() - line 326:
   local runtime=$(load_config runtime)
   $runtime run --rm "$image" -t "$tool" --help

3. In run_testsuite() - line 431-432:
   local runtime=$(load_config runtime)
   local docker_cmd=(
       $runtime run --rm --privileged
       ...

4. In list_docker_images() - line 90 and 93:
   local runtime=$(load_config runtime)
   $runtime images --format ...

Replace docker binary invocations, not docker-related strings like "docker_cmd" variable names.
  </action>
  <verify>
    grep -v "load_config.*runtime" /home/jenkins/.local/bin/dingofs-testsuite-tool | grep -c "^[^#]*\bdocker\b"
  </verify>
  <done>All docker binary invocations replaced with runtime variable</done>
</task>

</tasks>

<verification>
- `dtt config set runtime podman && dtt config show` shows "runtime: podman"
- `dtt config set runtime docker && dtt config show` shows "runtime: docker"
- Script functions correctly with docker runtime
- No hardcoded "docker " (with space) in command invocations
</verification>

<success_criteria>
- User can set runtime via `dtt config set runtime docker|podman`
- All docker commands respect the configured runtime
- Default is docker when not set (backward compatible)
- Help text includes runtime config example
</success_criteria>

<output>
After completion, create `.planning/quick/260428-fay-add-container-runtime-config-option-dock/{phase}-{plan}-SUMMARY.md`
</output>
