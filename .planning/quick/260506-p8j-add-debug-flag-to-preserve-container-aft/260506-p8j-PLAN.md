# Quick Task 260506-p8j: add --debug flag to preserve container after test

## Objective
Add `--debug` flag to `run_testsuite()`. When passed, omit `--rm` from docker run so the container stays alive for investigation.

## Tasks

### Task 1: Add --debug parameter parsing and conditional --rm

**Files:** `dingofs-testsuite-tool`

**Changes:**
1. Add `debug=""` local variable in run_testsuite()
2. Add `--debug)` case in argument parsing while loop
3. Conditionally build docker_cmd with/without `--rm` (general flow + mlperf flow)
4. Add `--debug` to show_help() options
5. Add `--debug` to main entry point case pattern

**Behavior:**
- `dtt -t int -s quota` → `docker run --rm ...` (container deleted after exit)
- `dtt -t int -s quota --debug` → `docker run ...` (container preserved)
